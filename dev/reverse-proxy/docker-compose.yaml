version: '3'

services:
  nginx:
    container_name: nginx
    image: nginx:1.23.4-alpine
    ports:
      - 80:80
      - 443:443
    volumes:
      - /etc/docker-volumes/nginx/nginx.conf.d/:/etc/nginx/nginx.conf.d/:ro